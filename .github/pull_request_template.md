## What does this change?

<!-- A short description. Link any issue it closes. -->

## Checklist

- [ ] `cargo test` passes
- [ ] `cargo clippy --all-targets -- -D warnings` is clean
- [ ] `cargo fmt --all -- --check` is clean
- [ ] If behaviour differs from the Go implementation, the README's
      "Differences from the Go implementation" section says so and why
